{% set name = "mdacli" %}
{% set version = "0.2.0" %}
{% set python_min = "3.11" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/mdacli-{{ version }}.tar.gz
  sha256: 40c29dd303d4bea4295805d2556ad769a25afad83c2573975f4ed6d1938825e1

build:
  entry_points:
    - mda = mdacli.__main__:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - setuptools >=77
    - setuptools_scm >=8
    - griddataformats==1.1.0=pyhd8ed1ab_1
    - wheel
    - pip
  run:
    - python >={{ python_min }}
    - mdanalysis >=2.10.0
    - threadpoolctl
    - argcomplete

test:
  imports:
    - mdacli
  commands:
    - pip check
    - mda --help
  requires:
    - python {{ python_min }}
    - pip

about:
  home: https://github.com/MDAnalysis/mdacli
  summary: A command line client for MDAnalysis Analysis classes.
  license: GPL-3.0-only
  license_file: LICENSE
  description: |
    mdacli is a simple command line interface (CLI) to the analysis
    classes of MDAnalysis using argparse.
  doc_url: https://mdacli.mdanalysis.org
  dev_url: https://github.com/MDAnalysis/mdacli

extra:
  recipe-maintainers:
    - hejamu
    - PicoCentauri
    - hejamu
